extends layouts/main.pug
include mixin/postBlock.pug

block content 
  .posts 
      each item in posts 
        +postBlock({
          id:item.id,
          title:item.title,
          author:item.author,
          createAt: item.createAt
        })
